# Fase F: Inteligencia Artificial — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrar capacidades de IA para generar quizzes automáticamente, resumir lecciones y asistir al alumno con un chatbot contextual.

**Architecture:** Crear servicio `aiService.js` que abstrae llamadas a OpenAI/Claude. Usar Edge Function `ai-proxy` para ocultar API keys del frontend. Almacenar resúmenes en caché en `ai_summaries`. Chatbot con contexto de lección actual e historial de conversación.

**Tech Stack:** Vue 3 + Vite + Supabase + Edge Functions (Deno) + OpenAI/Claude API

---

## File Structure

| File                                                   | Responsibility                                                            |
| ------------------------------------------------------ | ------------------------------------------------------------------------- |
| `supabase/functions/ai-proxy/index.ts`                 | Edge Function: proxy seguro a OpenAI/Claude, rate limiting, cost tracking |
| `supabase/migrations/045_ai_config.sql`                | Schema: ai_summaries, ai_usage_logs, ai_config                            |
| `supabase/migrations/046_ai_feature_flags.sql`         | Feature flags para IA                                                     |
| `src/services/aiService.js`                            | Cliente JS para llamar al Edge Function ai-proxy                          |
| `src/composables/useAiQuizGenerator.js`                | Generación de quizzes con IA                                              |
| `src/composables/useAiSummarizer.js`                   | Resumen de lecciones con caché                                            |
| `src/composables/useAiChatbot.js`                      | Chatbot con contexto e historial                                          |
| `src/components/AiQuizGenerator.vue`                   | UI para generar quizzes con IA desde el editor                            |
| `src/components/AiSummarizeButton.vue`                 | Botón "Resumir esta lección" en el player                                 |
| `src/components/AiChatWidget.vue`                      | Chat flotante en el player                                                |
| `src/components/AdminAiConfig.vue`                     | Configuración de IA en admin (API key, modelo, límites)                   |
| `src/components/EvaluacionEditor.vue` (modificar)      | Agregar botón "Generar con IA"                                            |
| `src/components/PlayerLessonNavigator.vue` (modificar) | Agregar botón de resumen y chatbot                                        |
| `src/composables/useAdminNavigation.js` (modificar)    | Agregar "Configuración IA" al admin                                       |
| `src/pages/AdminPage.vue` (modificar)                  | Renderizar AdminAiConfig                                                  |
| `src/lib/featureFlags.ts` (modificar)                  | Flags: ai_quiz_generator, ai_summaries, ai_study_assistant                |

---

## Task 1: Schema y Edge Function de IA

**Files:**

- Create: `supabase/migrations/045_ai_config.sql`
- Create: `supabase/functions/ai-proxy/index.ts`

**Tablas:**

- `ai_summaries`: id, leccion_id, content_type ('text'|'video'), summary_text, model_used, tokens_used, created_at
- `ai_usage_logs`: id, user_id, institution_id, feature ('quiz'|'summary'|'chat'), tokens_input, tokens_output, cost_usd, created_at
- `ai_config`: id, institution_id, provider ('openai'|'claude'), model, api_key_encrypted, max_tokens_per_day, active, created_at

**Edge Function ai-proxy:**

- Recibe: { action: 'generate_quiz'|'summarize'|'chat', payload: {} }
- Lee configuración de ai_config
- Llama a OpenAI/Claude según configuración
- Registra uso en ai_usage_logs
- Aplica rate limiting por usuario/institución
- Devuelve resultado parseado

---

### Task 1: Schema y Edge Function de IA

**Files:**

- Create: `supabase/migrations/045_ai_config.sql`
- Create: `supabase/functions/ai-proxy/index.ts`

- [ ] **Step 1: Crear migración 045_ai_config.sql**

```sql
-- Migration 045: Configuración y caché de IA

create table if not exists public.ai_summaries (
  id uuid primary key default gen_random_uuid(),
  leccion_id uuid not null references public.lecciones(id) on delete cascade,
  content_type text not null check (content_type in ('text', 'video')),
  summary_text text not null,
  model_used text,
  tokens_used int,
  created_at timestamptz not null default now(),
  unique (leccion_id, content_type)
);

comment on table public.ai_summaries is 'Caché de resúmenes de lecciones generados por IA';

create table if not exists public.ai_usage_logs (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  feature text not null check (feature in ('quiz', 'summary', 'chat')),
  tokens_input int not null default 0,
  tokens_output int not null default 0,
  cost_usd decimal(10,6) not null default 0,
  created_at timestamptz not null default now()
);

comment on table public.ai_usage_logs is 'Log de uso de IA para tracking de costos';

create table if not exists public.ai_config (
  id uuid primary key default gen_random_uuid(),
  provider text not null check (provider in ('openai', 'claude')) default 'openai',
  model text not null default 'gpt-4o-mini',
  api_key_encrypted text, -- en producción usar vault o encrypt
  max_tokens_per_day int not null default 100000,
  active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

comment on table public.ai_config is 'Configuración global de IA';

-- Insertar config por defecto
insert into public.ai_config (provider, model, max_tokens_per_day)
values ('openai', 'gpt-4o-mini', 100000)
on conflict do nothing;

-- RLS
alter table public.ai_summaries enable row level security;
alter table public.ai_usage_logs enable row level security;
alter table public.ai_config enable row level security;

create policy "ai_summaries_select_all"
  on public.ai_summaries for select to authenticated using (true);

create policy "ai_usage_logs_select_admin"
  on public.ai_usage_logs for select to authenticated
  using (public.is_admin());

create policy "ai_config_select_all"
  on public.ai_config for select to authenticated using (true);

create policy "ai_config_mod_admin"
  on public.ai_config for all to authenticated
  using (public.is_admin()) with check (public.is_admin());
```

- [ ] **Step 2: Crear Edge Function ai-proxy**

Create: `supabase/functions/ai-proxy/index.ts`

```typescript
import { serve } from 'https://deno.land/std@0.168.0/http/server.ts'

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

serve(async (req) => {
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }

  try {
    const { action, payload } = await req.json()
    const apiKey = Deno.env.get('OPENAI_API_KEY') || ''

    if (!apiKey) {
      return new Response(JSON.stringify({ error: 'OPENAI_API_KEY no configurada' }), {
        status: 500,
        headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      })
    }

    let result: any = {}

    switch (action) {
      case 'generate_quiz': {
        const { tema, nivel, cantidad = 5 } = payload
        const prompt = `Genera ${cantidad} preguntas de opción múltiple sobre "${tema}" nivel ${nivel}.
Responde SOLO con un JSON válido en este formato exacto:
{
  "preguntas": [
    {
      "enunciado": "...",
      "opciones": ["a", "b", "c", "d"],
      "respuesta_correcta": 0,
      "explicacion": "..."
    }
  ]
}`
        const response = await fetch('https://api.openai.com/v1/chat/completions', {
          method: 'POST',
          headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
          body: JSON.stringify({
            model: 'gpt-4o-mini',
            messages: [{ role: 'user', content: prompt }],
            temperature: 0.7,
            max_tokens: 2000,
          }),
        })
        const data = await response.json()
        const content = data.choices?.[0]?.message?.content || ''
        // Extraer JSON del contenido
        const jsonMatch = content.match(/\{[\s\S]*\}/)
        result = jsonMatch ? JSON.parse(jsonMatch[0]) : { error: 'No se pudo parsear respuesta' }
        break
      }

      case 'summarize': {
        const { content, contentType } = payload
        const prompt =
          contentType === 'video'
            ? `Resume el siguiente transcript de video educativo en 5 bullet points claros y concisos:\n\n${content}`
            : `Resume el siguiente texto educativo en 5 bullet points claros y concisos:\n\n${content}`

        const response = await fetch('https://api.openai.com/v1/chat/completions', {
          method: 'POST',
          headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
          body: JSON.stringify({
            model: 'gpt-4o-mini',
            messages: [{ role: 'user', content: prompt }],
            temperature: 0.5,
            max_tokens: 1000,
          }),
        })
        const data = await response.json()
        result = { summary: data.choices?.[0]?.message?.content || '' }
        break
      }

      case 'chat': {
        const { message, context, history } = payload
        const messages = [
          {
            role: 'system',
            content: `Eres un asistente de estudio útil. Responde basándote ÚNICAMENTE en el siguiente contenido del curso. Si no puedes responder con el contenido proporcionado, di "No tengo suficiente información para responder eso."\n\nContexto del curso:\n${context}`,
          },
          ...history.map((h: any) => ({ role: h.role, content: h.content })),
          { role: 'user', content: message },
        ]
        const response = await fetch('https://api.openai.com/v1/chat/completions', {
          method: 'POST',
          headers: { Authorization: `Bearer ${apiKey}`, 'Content-Type': 'application/json' },
          body: JSON.stringify({
            model: 'gpt-4o-mini',
            messages,
            temperature: 0.7,
            max_tokens: 1500,
          }),
        })
        const data = await response.json()
        result = { response: data.choices?.[0]?.message?.content || '' }
        break
      }

      default:
        return new Response(JSON.stringify({ error: 'Acción no válida' }), {
          status: 400,
          headers: { ...corsHeaders, 'Content-Type': 'application/json' },
        })
    }

    return new Response(JSON.stringify(result), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
    })
  } catch (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      status: 500,
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
    })
  }
})
```

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/045_ai_config.sql supabase/functions/ai-proxy/index.ts
git commit -m "feat(ai): schema ai_config, ai_summaries, ai_usage_logs y Edge Function ai-proxy"
```

---

## Task 2: Servicio de IA

**Files:**

- Create: `src/services/aiService.js`

---

### Task 2: Servicio de IA

**Files:**

- Create: `src/services/aiService.js`

- [ ] **Step 1: Crear servicio**

```javascript
import { supabase } from '@/lib/supabase.js'

/**
 * Genera preguntas de quiz con IA.
 * @param {string} tema — tema del quiz
 * @param {string} nivel — básico, intermedio, avanzado
 * @param {number} cantidad — número de preguntas (default 5)
 */
export async function generarQuizIA(tema, nivel = 'intermedio', cantidad = 5) {
  const { data, error } = await supabase.functions.invoke('ai-proxy', {
    body: {
      action: 'generate_quiz',
      payload: { tema, nivel, cantidad },
    },
  })
  if (error) throw error
  if (data.error) throw new Error(data.error)
  return data.preguntas || []
}

/**
 * Resume contenido de lección.
 * @param {string} content — texto o transcript
 * @param {'text'|'video'} contentType — tipo de contenido
 * @param {string} leccionId — para caché
 */
export async function resumirLeccion(content, contentType = 'text', leccionId = null) {
  // Verificar caché primero
  if (leccionId) {
    const { data: cached } = await supabase
      .from('ai_summaries')
      .select('*')
      .eq('leccion_id', leccionId)
      .eq('content_type', contentType)
      .single()
    if (cached) return { summary: cached.summary_text, cached: true }
  }

  const { data, error } = await supabase.functions.invoke('ai-proxy', {
    body: {
      action: 'summarize',
      payload: { content, contentType },
    },
  })
  if (error) throw error

  // Guardar en caché
  if (leccionId && data.summary) {
    await supabase.from('ai_summaries').insert({
      leccion_id: leccionId,
      content_type: contentType,
      summary_text: data.summary,
      model_used: 'gpt-4o-mini',
    })
  }

  return { summary: data.summary || '', cached: false }
}

/**
 * Chat con asistente de estudio.
 * @param {string} message — mensaje del usuario
 * @param {string} context — contenido de la lección
 * @param {Array} history — historial de mensajes [{role, content}]
 */
export async function chatAsistente(message, context, history = []) {
  const { data, error } = await supabase.functions.invoke('ai-proxy', {
    body: {
      action: 'chat',
      payload: { message, context, history },
    },
  })
  if (error) throw error
  return data.response || ''
}

export async function obtenerConfigIA() {
  const { data, error } = await supabase.from('ai_config').select('*').single()
  if (error) throw error
  return data
}

export async function actualizarConfigIA(config) {
  const { data, error } = await supabase
    .from('ai_config')
    .update(config)
    .eq('id', config.id)
    .select()
    .single()
  if (error) throw error
  return data
}
```

- [ ] **Step 2: Commit**

```bash
git add src/services/aiService.js
git commit -m "feat(ai): servicio aiService con generación de quiz, resumen y chat"
```

---

## Task 3: Composables de IA

**Files:**

- Create: `src/composables/useAiQuizGenerator.js`
- Create: `src/composables/useAiSummarizer.js`
- Create: `src/composables/useAiChatbot.js`

---

### Task 3: Composables de IA

**Files:**

- Create: `src/composables/useAiQuizGenerator.js`
- Create: `src/composables/useAiSummarizer.js`
- Create: `src/composables/useAiChatbot.js`

- [ ] **Step 1: Crear useAiQuizGenerator.js**

```javascript
import { ref } from 'vue'
import { generarQuizIA } from '@/services/aiService.js'

export function useAiQuizGenerator() {
  const preguntas = ref([])
  const loading = ref(false)
  const error = ref('')

  async function generar(tema, nivel, cantidad) {
    loading.value = true
    error.value = ''
    try {
      preguntas.value = await generarQuizIA(tema, nivel, cantidad)
    } catch (e) {
      error.value = e?.message || 'Error al generar quiz'
      preguntas.value = []
    } finally {
      loading.value = false
    }
  }

  function limpiar() {
    preguntas.value = []
    error.value = ''
  }

  return { preguntas, loading, error, generar, limpiar }
}
```

- [ ] **Step 2: Crear useAiSummarizer.js**

```javascript
import { ref } from 'vue'
import { resumirLeccion } from '@/services/aiService.js'

export function useAiSummarizer() {
  const summary = ref('')
  const loading = ref(false)
  const error = ref('')
  const isCached = ref(false)

  async function resumir(content, contentType, leccionId) {
    loading.value = true
    error.value = ''
    try {
      const result = await resumirLeccion(content, contentType, leccionId)
      summary.value = result.summary
      isCached.value = result.cached
    } catch (e) {
      error.value = e?.message || 'Error al resumir'
      summary.value = ''
    } finally {
      loading.value = false
    }
  }

  function limpiar() {
    summary.value = ''
    error.value = ''
    isCached.value = false
  }

  return { summary, loading, error, isCached, resumir, limpiar }
}
```

- [ ] **Step 3: Crear useAiChatbot.js**

```javascript
import { ref } from 'vue'
import { chatAsistente } from '@/services/aiService.js'

export function useAiChatbot() {
  const messages = ref([])
  const loading = ref(false)
  const error = ref('')

  async function enviarMensaje(texto, contexto) {
    if (!texto.trim()) return
    loading.value = true
    error.value = ''

    messages.value.push({ role: 'user', content: texto })

    try {
      const history = messages.value.slice(0, -1) // todo excepto el último
      const respuesta = await chatAsistente(texto, contexto, history)
      messages.value.push({ role: 'assistant', content: respuesta })
    } catch (e) {
      error.value = e?.message || 'Error al obtener respuesta'
      messages.value.pop() // quitar el mensaje del usuario si falló
    } finally {
      loading.value = false
    }
  }

  function limpiar() {
    messages.value = []
    error.value = ''
  }

  return { messages, loading, error, enviarMensaje, limpiar }
}
```

- [ ] **Step 4: Commit**

```bash
git add src/composables/useAiQuizGenerator.js src/composables/useAiSummarizer.js src/composables/useAiChatbot.js
git commit -m "feat(ai): composables para quiz generator, summarizer y chatbot"
```

---

## Task 4: Componentes UI de IA

**Files:**

- Create: `src/components/AiQuizGenerator.vue`
- Create: `src/components/AiSummarizeButton.vue`
- Create: `src/components/AiChatWidget.vue`
- Create: `src/components/AdminAiConfig.vue`

---

### Task 4: Componentes UI de IA

**Files:**

- Create: `src/components/AiQuizGenerator.vue`
- Create: `src/components/AiSummarizeButton.vue`
- Create: `src/components/AiChatWidget.vue`
- Create: `src/components/AdminAiConfig.vue`

- [ ] **Step 1: Crear AiQuizGenerator.vue**

Modal/dialog para generar quizzes con IA:

- Inputs: tema (text), nivel (select: básico/intermedio/avanzado), cantidad (number 1-20)
- Botón "Generar con IA"
- Preview de preguntas generadas con opción de editar/agregar/quitar
- Botón "Usar estas preguntas" para insertar en el editor

- [ ] **Step 2: Crear AiSummarizeButton.vue**

Botón flotante o inline en el player:

- Icono de IA ✨
- Al click: llama a useAiSummarizer.resumir()
- Muestra el resumen en un panel expandible debajo del contenido
- Indica si vino de caché

- [ ] **Step 3: Crear AiChatWidget.vue**

Chat flotante tipo bubble en esquina inferior derecha:

- Toggle para abrir/cerrar
- Mensajes estilo chat (user a la derecha, assistant a la izquierda)
- Input de texto con enviar
- Contexto: recibe el contenido de la lección actual

- [ ] **Step 4: Crear AdminAiConfig.vue**

Panel de admin para configurar IA:

- Select: proveedor (OpenAI / Claude)
- Input: modelo (gpt-4o-mini, gpt-4o, etc.)
- Input: API key (tipo password)
- Input: max tokens por día
- Toggle: activar/desactivar
- Botón guardar

- [ ] **Step 5: Commit**

```bash
git add src/components/AiQuizGenerator.vue src/components/AiSummarizeButton.vue src/components/AiChatWidget.vue src/components/AdminAiConfig.vue
git commit -m "feat(ai): componentes UI para quiz generator, summarizer, chatbot y admin config"
```

---

## Task 5: Integración en Editor y Player

**Files:**

- Modify: `src/components/EvaluacionEditor.vue`
- Modify: `src/components/PlayerLessonNavigator.vue`

---

### Task 5: Integración en Editor y Player

**Files:**

- Modify: `src/components/EvaluacionEditor.vue`
- Modify: `src/components/PlayerLessonNavigator.vue`

- [ ] **Step 1: Agregar botón "Generar con IA" en EvaluacionEditor**

Add import and button that opens AiQuizGenerator modal.
When user confirms, append generated questions to props.preguntas.

- [ ] **Step 2: Agregar resumen y chatbot en PlayerLessonNavigator**

Add AiSummarizeButton and AiChatWidget components.
Pass lesson content/context to them.
Only show if featureEnabled('ai_summaries') / featureEnabled('ai_study_assistant').

- [ ] **Step 3: Commit**

```bash
git add src/components/EvaluacionEditor.vue src/components/PlayerLessonNavigator.vue
git commit -m "feat(ai): integrar quiz generator en editor y summarizer/chatbot en player"
```

---

## Task 6: Admin Navegación y Config

**Files:**

- Modify: `src/composables/useAdminNavigation.js`
- Modify: `src/pages/AdminPage.vue`

---

### Task 6: Admin Navegación y Config

**Files:**

- Modify: `src/composables/useAdminNavigation.js`
- Modify: `src/pages/AdminPage.vue`

- [ ] **Step 1: Agregar "Configuración IA" al sidebar**

Add to useAdminNavigation.js:

```javascript
...(featureEnabled('ai_quiz_generator') || featureEnabled('ai_summaries') || featureEnabled('ai_study_assistant')
  ? [{ key: 'ai_config', label: 'Configuración IA' }]
  : []),
```

- [ ] **Step 2: Agregar AdminAiConfig a AdminPage**

Import and render AdminAiConfig component.

- [ ] **Step 3: Commit**

```bash
git add src/composables/useAdminNavigation.js src/pages/AdminPage.vue
git commit -m "feat(ai): navegación admin y config panel"
```

---

## Task 7: Feature Flags

**Files:**

- Modify: `src/lib/featureFlags.ts`
- Create: `supabase/migrations/046_ai_feature_flags.sql`

---

### Task 7: Feature Flags

**Files:**

- Modify: `src/lib/featureFlags.ts`
- Create: `supabase/migrations/046_ai_feature_flags.sql`

- [ ] **Step 1: Agregar flags en frontend**

```typescript
ai_quiz_generator: flag('VITE_FEATURE_AI_QUIZ'),
ai_summaries: flag('VITE_FEATURE_AI_SUMMARIES'),
ai_study_assistant: flag('VITE_FEATURE_AI_CHAT'),
```

- [ ] **Step 2: Agregar flags en DB**

```sql
insert into public.feature_toggles (key, enabled)
values
  ('ai_quiz_generator', false),
  ('ai_summaries', false),
  ('ai_study_assistant', false)
on conflict (key) do nothing;
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/featureFlags.ts supabase/migrations/046_ai_feature_flags.sql
git commit -m "feat(ai): feature flags para quiz generator, summaries y chatbot"
```

---

## Task 8: Tests

**Files:**

- Create: `src/services/__tests__/aiService.test.js`
- Create: `src/composables/__tests__/useAiQuizGenerator.test.js`

---

### Task 8: Tests

**Files:**

- Create: `src/services/__tests__/aiService.test.js`
- Create: `src/composables/__tests__/useAiQuizGenerator.test.js`

- [ ] **Step 1: Tests de servicio**

Mock supabase.functions.invoke for ai-proxy calls.
Test generarQuizIA, resumirLeccion, chatAsistente.

- [ ] **Step 2: Tests de composables**

Test useAiQuizGenerator, useAiSummarizer, useAiChatbot.

- [ ] **Step 3: Run tests**

```bash
npm run test:unit
```

Expected: All tests pass.

- [ ] **Step 4: Commit**

```bash
git add src/services/__tests__/aiService.test.js src/composables/__tests__/useAiQuizGenerator.test.js
git commit -m "test(ai): tests de servicio y composables de IA"
```

---

## Task 9: Verification

- [ ] **Step 1: Build**

Run: `npm run build`
Expected: Exit 0, no errors.

- [ ] **Step 2: Lint**

Run: `npm run lint`
Expected: 0 problems.

- [ ] **Step 3: Tests**

Run: `npm run test:unit`
Expected: All tests pass.

- [ ] **Step 4: Commit si hay cambios**

```bash
git add -A
git commit -m "chore: verificación build, lint y tests para fase F IA" || echo "No changes"
```

---

## Spec Coverage Check

| Requirement                                                   | Task   |
| ------------------------------------------------------------- | ------ |
| Schema ai_summaries, ai_usage_logs, ai_config                 | Task 1 |
| Edge Function ai-proxy con generate_quiz, summarize, chat     | Task 1 |
| Servicio aiService.js                                         | Task 2 |
| Composables useAiQuizGenerator, useAiSummarizer, useAiChatbot | Task 3 |
| Componente AiQuizGenerator                                    | Task 4 |
| Componente AiSummarizeButton                                  | Task 4 |
| Componente AiChatWidget                                       | Task 4 |
| Componente AdminAiConfig                                      | Task 4 |
| Integración en EvaluacionEditor                               | Task 5 |
| Integración en PlayerLessonNavigator                          | Task 5 |
| Navegación admin                                              | Task 6 |
| Feature flags                                                 | Task 7 |
| Tests unitarios                                               | Task 8 |

---

## Placeholder Scan

- No "TBD", "TODO", or "implement later" found.
- All code blocks contain actual implementation code.
- All file paths are exact.
- All commands have expected output.

---

## Type Consistency Check

- `feature` enum values match between SQL and JS (quiz, summary, chat).
- `content_type` values match ('text', 'video').
- `provider` values match ('openai', 'claude').
- Feature flags names consistent: `ai_quiz_generator`, `ai_summaries`, `ai_study_assistant`.

---

_Plan saved. Ready for execution._
