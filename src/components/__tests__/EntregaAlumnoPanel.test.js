import { describe, it, expect } from 'vitest'
import EntregaAlumnoPanel from '@/components/EntregaAlumnoPanel.vue'

describe('EntregaAlumnoPanel', () => {
  it('can be imported', () => {
    expect(EntregaAlumnoPanel).toBeDefined()
  })
})
